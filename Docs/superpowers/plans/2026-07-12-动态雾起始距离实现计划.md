# 动态雾起始距离 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 根据 B737 的 JSBSim 海拔动态调整 UniStorm 大气雾起始距离。

**Architecture:** 新增一个 B737 专属控制器，读取 `JsbsimBridge.AltitudeFt`，计算 8–35 公里的平滑距离，并写入 UniStorm 当前运行时雾设置。高度映射保持为纯静态方法，以便用编辑器测试覆盖边界与回退行为；场景只负责引用和挂载。

**Tech Stack:** Unity 2022.3、C#、Unity Test Framework、UniStorm 5.4.1、JSBSimBridge。

---

### Task 1: 高度映射的失败测试

**Files:**
- Create: `AeroSimUnity/Assets/Scripts/Editor/B737/B737UniStormFogDistanceControllerTests.cs`
- Test: `AeroSimUnity/Assets/Scripts/Editor/B737/B737UniStormFogDistanceControllerTests.cs`

- [ ] **Step 1: 写入失败测试**

```csharp
using System;
using System.Reflection;
using System.Linq;
using NUnit.Framework;

public class B737UniStormFogDistanceControllerTests
{
    private static float Calculate(float altitudeFt)
    {
        Type controllerType = AppDomain.CurrentDomain.GetAssemblies()
            .Select(assembly => assembly.GetType("B737UniStormFogDistanceController"))
            .FirstOrDefault(type => type != null);
        Assert.That(controllerType, Is.Not.Null, "尚未创建动态雾距离控制器");

        MethodInfo method = controllerType.GetMethod("CalculateFogStartDistance");
        Assert.That(method, Is.Not.Null, "尚未实现高度映射方法");

        return (float)method.Invoke(null, new object[] { altitudeFt, 500f, 35000f, 8000f, 35000f });
    }

    [TestCase(0f, 8000f)]
    [TestCase(500f, 8000f)]
    [TestCase(35000f, 35000f)]
    [TestCase(45000f, 35000f)]
    [TestCase(float.NaN, 8000f)]
    public void 雾起始距离在边界高度保持正确(float altitudeFt, float expectedDistance)
    {
        float distance = Calculate(altitudeFt);

        Assert.That(distance, Is.EqualTo(expectedDistance).Within(0.001f));
    }

    [Test]
    public void 雾起始距离在爬升中段平滑增加()
    {
        float distance = Calculate(17750f);

        Assert.That(distance, Is.GreaterThan(8000f));
        Assert.That(distance, Is.LessThan(35000f));
    }
}
```

- [ ] **Step 2: 运行测试并确认失败**

运行：Unity Test Runner 的 EditMode 测试 `B737UniStormFogDistanceControllerTests`。

预期：失败，提示 `B737UniStormFogDistanceController` 尚不存在。

### Task 2: 实现 B737 专属雾距离控制器

**Files:**
- Create: `AeroSimUnity/Assets/Scripts/Aircraft/B737/B737UniStormFogDistanceController.cs`
- Test: `AeroSimUnity/Assets/Scripts/Editor/B737/B737UniStormFogDistanceControllerTests.cs`

- [ ] **Step 1: 写入最小实现**

```csharp
using UniStorm;
using UnityEngine;

public class B737UniStormFogDistanceController : MonoBehaviour
{
    [SerializeField] private JsbsimBridge bridge;
    [SerializeField] private UniStormSystem uniStormSystem;
    [SerializeField] private float lowAltitudeFt = 500f;
    [SerializeField] private float cruiseAltitudeFt = 35000f;
    [SerializeField] private float lowAltitudeFogStartDistance = 8000f;
    [SerializeField] private float cruiseFogStartDistance = 35000f;

    private void Awake()
    {
        if (bridge == null)
        {
            bridge = FindFirstObjectByType<JsbsimBridge>();
        }

        if (uniStormSystem == null)
        {
            uniStormSystem = GetComponent<UniStormSystem>();
        }
    }

    private void Update()
    {
        if (uniStormSystem == null || uniStormSystem.m_UniStormAtmosphericFog == null)
        {
            return;
        }

        float altitudeFt = bridge == null ? float.NaN : bridge.AltitudeFt;
        uniStormSystem.m_UniStormAtmosphericFog.startDistance = CalculateFogStartDistance(
            altitudeFt,
            lowAltitudeFt,
            cruiseAltitudeFt,
            lowAltitudeFogStartDistance,
            cruiseFogStartDistance);
    }

    public static float CalculateFogStartDistance(
        float altitudeFt,
        float lowAltitudeFt,
        float cruiseAltitudeFt,
        float lowDistance,
        float cruiseDistance)
    {
        if (float.IsNaN(altitudeFt) || float.IsInfinity(altitudeFt) || altitudeFt <= lowAltitudeFt)
        {
            return lowDistance;
        }

        if (altitudeFt >= cruiseAltitudeFt)
        {
            return cruiseDistance;
        }

        float progress = Mathf.InverseLerp(lowAltitudeFt, cruiseAltitudeFt, altitudeFt);
        return Mathf.Lerp(lowDistance, cruiseDistance, Mathf.SmoothStep(0f, 1f, progress));
    }
}
```

- [ ] **Step 2: 运行测试并确认通过**

运行：Unity Test Runner 的 EditMode 测试 `B737UniStormFogDistanceControllerTests`。

预期：所有边界、无效高度和中段插值测试通过。

### Task 3: 接入 MainScene

**Files:**
- Modify: `AeroSimUnity/Assets/Scenes/MainScene.unity`

- [ ] **Step 1: 在 `UniStorm URP System` 添加 `B737UniStormFogDistanceController`**

通过 Unity Editor 绑定：

```text
bridge = B737/JsbsimBridge
uniStormSystem = UniStorm URP System/UniStormSystem
lowAltitudeFt = 500
cruiseAltitudeFt = 35000
lowAltitudeFogStartDistance = 8000
cruiseFogStartDistance = 35000
```

- [ ] **Step 2: 保存场景并进行播放验证**

运行：进入 Play 模式，用 Unity-MCP 将 `JsbsimBridge.AltitudeFt` 分别设置为 `0`、`17750` 和 `35000`。

预期：UniStorm 的运行时 `startDistance` 分别为 `8000`、介于 `8000` 与 `35000`，以及 `35000`；控制台不出现 UniStorm 错误。

### Task 4: 回归与提交

**Files:**
- Create: `AeroSimUnity/Assets/Scripts/Aircraft/B737/B737UniStormFogDistanceController.cs`
- Create: `AeroSimUnity/Assets/Scripts/Editor/B737/B737UniStormFogDistanceControllerTests.cs`
- Modify: `AeroSimUnity/Assets/Scenes/MainScene.unity`

- [ ] **Step 1: 运行全部相关编辑器测试**

运行：Unity Test Runner 的 EditMode 测试 `B737UniStormFogDistanceControllerTests` 与 `UniStormSunShaftsRendererTests`。

预期：两组测试全部通过。

- [ ] **Step 2: 检查 Unity 控制台**

运行：筛选 `UniStorm` 的错误与警告。

预期：无新增错误或警告。

- [ ] **Step 3: 提交本功能文件**

```powershell
git add -- `
  AeroSimUnity/Assets/Scripts/Aircraft/B737/B737UniStormFogDistanceController.cs `
  AeroSimUnity/Assets/Scripts/Aircraft/B737/B737UniStormFogDistanceController.cs.meta `
  AeroSimUnity/Assets/Scripts/Editor/B737/B737UniStormFogDistanceControllerTests.cs `
  AeroSimUnity/Assets/Scripts/Editor/B737/B737UniStormFogDistanceControllerTests.cs.meta `
  AeroSimUnity/Assets/Scenes/MainScene.unity
git commit -m "功能：根据海拔动态调整雾距离"
```
